

<h2>User info</h2>

<%#= attachment_image_tag user, :profile_image, size: "100", format: "jpg", fallback: "no_image.jpg" %>
<%= attachment_image_tag user, :profile_image, size: "100", fallback: "no_image.jpg" %>
<table class="table">
  <tbody><tr></tr>
  <tr>
	  <th>name</th>
	  <th><%=user.name%></th>
  </tr>
  <tr>
	  <th>introduction</th>
	  <th><%= user.introduction%></th>
  </tr>
</tbody></table>
<%# if false%>
<%#= link_to , edit_user_path(user.id) %>
<div class="row">
<%= link_to "",edit_user_path(@user.id), class: 'btn btn-outline-secondary btn-block fas fa-user-cog ' %>
</div>
<%# end %>