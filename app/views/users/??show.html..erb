<h2>User info</h2>

<%= attachment_image_tag @user, :profile_image, size: "30×30", format: "jpg", fallback: "no_image.jpg" %>
<p>Name          <%= @user.name %></p>
<p>Introduction  <%= @user.introduction %></p>

<% if false%>
<%= link_to 'EDIT', edit_user_path(current_user.id) %>
<% end %>

<h2>New book</h2>

<%# if false %>

<%= form_with model: @book, local:true do |f| %>
  <h4>Title</h4>
  <%= f.text_field :title %>
  <h4>Opinion</h4>
  <%= f.text_area :body %>
  <%= f.submit 'Create Book' %>
<% end %>

<%# end %>


<h2>Books</h2>
<table>
  <tr>
    <th>Title</th>
    <th>Opinion</th>
  </tr>
  <% @books.each do |book|%>
  <tr>
    <td><%= book.title%></td>
    <td><%= book.body%></td>
  </tr>
</table>
  <%end%>

